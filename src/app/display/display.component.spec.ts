import { DisplayComponent } from './display.component';

describe('DisplayComponent', () => {
  it('should create the display', () => {
    let component = new DisplayComponent();
    expect(component).toBeTruthy();
  })
});
